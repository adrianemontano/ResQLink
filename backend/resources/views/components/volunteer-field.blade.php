@props(['label', 'name', 'type' => 'text', 'options' => [], 'placeholder' => null])
<div class="volunteer-field {{ $type === 'textarea' ? 'volunteer-field-full' : '' }}"><label for="{{ $name }}">{{ $label }}</label>@if ($type === 'select')<select id="{{ $name }}" name="{{ $name }}" @required($attributes->has('required'))><option value="">Select category</option>@foreach ($options as $option)<option value="{{ $option }}" @selected(old($name) === $option)>{{ $option }}</option>@endforeach</select>@elseif ($type === 'textarea')<textarea id="{{ $name }}" name="{{ $name }}" rows="4" placeholder="{{ $placeholder }}">{{ old($name) }}</textarea>@else<input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" value="{{ old($name) }}" placeholder="{{ $placeholder }}" {{ $attributes }}>@endif @error($name)<small>{{ $message }}</small>@enderror</div>
